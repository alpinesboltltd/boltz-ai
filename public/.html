<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Database Schema Normalizer</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.3.0/papaparse.min.js"></script>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          "Inter",
          -apple-system,
          BlinkMacSystemFont,
          sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
      }

      .container {
        max-width: 1600px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        overflow: hidden;
      }

      .header {
        background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
        color: white;
        padding: 30px;
        text-align: center;
      }

      .header h1 {
        font-size: 2.5rem;
        margin-bottom: 10px;
        font-weight: 700;
      }

      .header p {
        font-size: 1.1rem;
        opacity: 0.9;
      }

      .content {
        padding: 30px;
      }

      .section {
        margin-bottom: 40px;
        background: white;
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        border: 1px solid rgba(0, 0, 0, 0.05);
      }

      .section h2 {
        color: #4f46e5;
        margin-bottom: 20px;
        font-size: 1.5rem;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .icon {
        width: 24px;
        height: 24px;
        background: linear-gradient(135deg, #4f46e5, #7c3aed);
        border-radius: 6px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 14px;
      }

      .table-container {
        overflow-x: auto;
        margin: 20px 0;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      table {
        width: 100%;
        border-collapse: collapse;
        background: white;
        font-size: 0.85rem;
      }

      th {
        background: linear-gradient(135deg, #4f46e5, #7c3aed);
        color: white;
        padding: 12px 8px;
        text-align: left;
        font-weight: 600;
        font-size: 0.8rem;
        border: none;
      }

      td {
        padding: 10px 8px;
        border-bottom: 1px solid #e5e7eb;
        vertical-align: top;
        font-size: 0.8rem;
      }

      tr:hover {
        background: #f8fafc;
      }

      .code-block {
        background: #1e293b;
        color: #e2e8f0;
        padding: 20px;
        border-radius: 10px;
        font-family: "Monaco", "Menlo", monospace;
        font-size: 0.85rem;
        overflow-x: auto;
        margin: 15px 0;
        max-height: 400px;
        overflow-y: auto;
      }

      .sql-block {
        background: #0f172a;
        color: #cbd5e1;
        padding: 20px;
        border-radius: 10px;
        font-family: "Monaco", "Menlo", monospace;
        font-size: 0.85rem;
        overflow-x: auto;
        margin: 15px 0;
        max-height: 500px;
        overflow-y: auto;
      }

      .btn {
        background: linear-gradient(135deg, #4f46e5, #7c3aed);
        color: white;
        border: none;
        padding: 12px 24px;
        border-radius: 10px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.3s ease;
        margin: 10px 5px;
      }

      .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(79, 70, 229, 0.3);
      }

      .btn-secondary {
        background: linear-gradient(135deg, #10b981, #059669);
      }

      .btn-secondary:hover {
        box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
      }

      .btn-warning {
        background: linear-gradient(135deg, #f59e0b, #d97706);
      }

      .btn-warning:hover {
        box-shadow: 0 10px 25px rgba(245, 158, 11, 0.3);
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
        margin: 20px 0;
      }

      .card {
        background: white;
        border-radius: 12px;
        padding: 20px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        border: 1px solid rgba(0, 0, 0, 0.05);
      }

      .card h3 {
        color: #4f46e5;
        margin-bottom: 15px;
        font-size: 1.1rem;
      }

      .highlight {
        background: rgba(79, 70, 229, 0.1);
        padding: 2px 6px;
        border-radius: 4px;
        font-weight: 600;
      }

      .data-type {
        background: #f1f5f9;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.75rem;
        font-weight: 600;
        color: #475569;
      }

      .nullable {
        color: #ef4444;
        font-size: 0.75rem;
      }

      .not-null {
        color: #10b981;
        font-size: 0.75rem;
      }

      .tabs {
        display: flex;
        margin-bottom: 20px;
        border-bottom: 2px solid #e5e7eb;
      }

      .tab {
        padding: 12px 24px;
        cursor: pointer;
        background: none;
        border: none;
        font-weight: 600;
        color: #6b7280;
        transition: all 0.3s ease;
      }

      .tab.active {
        color: #4f46e5;
        border-bottom: 2px solid #4f46e5;
      }

      .tab-content {
        display: none;
      }

      .tab-content.active {
        display: block;
      }

      @media (max-width: 768px) {
        .content {
          padding: 20px;
        }

        .section {
          padding: 15px;
        }

        table {
          font-size: 0.75rem;
        }

        th,
        td {
          padding: 6px 4px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🗄️ Database Schema Normalizer</h1>
        <p>
          Normalize chatagent data into proper database schemas with TypeScript
          types and JSON Server format
        </p>
      </div>

      <div class="content">
        <!-- Schema Overview -->
        <div class="section">
          <h2><span class="icon">📋</span>Normalized Database Schema</h2>
          <p>
            Your original nested JSON has been normalized into 6 related tables
            following database normalization principles:
          </p>
          <div class="grid">
            <div class="card">
              <h3>🤖 chatagents</h3>
              <p>Main chatagent entities with core properties</p>
            </div>
            <div class="card">
              <h3>🎨 chatagent_appearance</h3>
              <p>UI/UX settings and styling configurations</p>
            </div>
            <div class="card">
              <h3>⚙️ chatagent_behavior</h3>
              <p>Behavior settings and message configurations</p>
            </div>
            <div class="card">
              <h3>🔗 chatagent_integrations</h3>
              <p>Platform integrations and API configurations</p>
            </div>
            <div class="card">
              <h3>📊 chatagent_stats</h3>
              <p>Performance metrics and analytics data</p>
            </div>
            <div class="card">
              <h3>🧠 training_data</h3>
              <p>Enhanced training content with vectorization metadata</p>
            </div>
          </div>
        </div>

        <!-- Tables Display -->
        <div class="section">
          <h2><span class="icon">📊</span>Normalized Tables</h2>

          <!-- Tabs -->
          <div class="tabs">
            <button class="tab active" onclick="showTab('chatagents')">
              Chatbots
            </button>
            <button class="tab" onclick="showTab('appearance')">
              Appearance
            </button>
            <button class="tab" onclick="showTab('behavior')">Behavior</button>
            <button class="tab" onclick="showTab('integrations')">
              Integrations
            </button>
            <button class="tab" onclick="showTab('stats')">Stats</button>
            <button class="tab" onclick="showTab('training')">
              Training Data
            </button>
          </div>

          <!-- Chatbots Table -->
          <div id="chatagents-tab" class="tab-content active">
            <h3>chatagents</h3>
            <div class="table-container">
              <table id="chatagentsTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>name</th>
                    <th>description</th>
                    <th>ai_model</th>
                    <th>status</th>
                    <th>created_at</th>
                    <th>updated_at</th>
                  </tr>
                </thead>
                <tbody id="chatagentsTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Appearance Table -->
          <div id="appearance-tab" class="tab-content">
            <h3>chatagent_appearance</h3>
            <div class="table-container">
              <table id="appearanceTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>chatagent_id</th>
                    <th>primary_color</th>
                    <th>font_family</th>
                    <th>chat_icon</th>
                    <th>welcome_message</th>
                    <th>created_at</th>
                    <th>updated_at</th>
                  </tr>
                </thead>
                <tbody id="appearanceTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Behavior Table -->
          <div id="behavior-tab" class="tab-content">
            <h3>chatagent_behavior</h3>
            <div class="table-container">
              <table id="behaviorTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>chatagent_id</th>
                    <th>initial_messages</th>
                    <th>fallback_message</th>
                    <th>enable_human_handoff</th>
                    <th>offline_message</th>
                    <th>created_at</th>
                    <th>updated_at</th>
                  </tr>
                </thead>
                <tbody id="behaviorTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Integrations Table -->
          <div id="integrations-tab" class="tab-content">
            <h3>chatagent_integrations</h3>
            <div class="table-container">
              <table id="integrationsTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>chatagent_id</th>
                    <th>platform</th>
                    <th>api_key</th>
                    <th>is_active</th>
                    <th>created_at</th>
                    <th>updated_at</th>
                  </tr>
                </thead>
                <tbody id="integrationsTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Stats Table -->
          <div id="stats-tab" class="tab-content">
            <h3>chatagent_stats</h3>
            <div class="table-container">
              <table id="statsTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>chatagent_id</th>
                    <th>total_messages</th>
                    <th>unique_users</th>
                    <th>average_rating</th>
                    <th>response_rate</th>
                    <th>conversions_count</th>
                    <th>last_calculated_at</th>
                  </tr>
                </thead>
                <tbody id="statsTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Training Data Table -->
          <div id="training-tab" class="tab-content">
            <h3>training_data</h3>
            <div class="table-container">
              <table id="trainingTable">
                <thead>
                  <tr>
                    <th>id</th>
                    <th>chatagent_id</th>
                    <th>content_type</th>
                    <th>category</th>
                    <th>title</th>
                    <th>content</th>
                    <th>intent</th>
                    <th>keywords</th>
                    <th>confidence_score</th>
                    <th>source_url</th>
                    <th>is_active</th>
                    <th>created_at</th>
                    <th>updated_at</th>
                  </tr>
                </thead>
                <tbody id="trainingTableBody"></tbody>
              </table>
            </div>
          </div>
        </div>

        <!-- SQL Schema -->
        <div class="section">
          <h2><span class="icon">🗃️</span>SQL Create Statements</h2>
          <p>Use these SQL statements to create your database tables:</p>
          <div class="sql-block" id="sqlSchema"></div>
          <button class="btn" onclick="copyToClipboard('sqlSchema')">
            📋 Copy SQL Schema
          </button>
        </div>

        <!-- TypeScript Types -->
        <div class="section">
          <h2><span class="icon">📝</span>TypeScript Type Definitions</h2>
          <p>Generated TypeScript interfaces for type-safe development:</p>
          <div class="code-block" id="tsTypes"></div>
          <button
            class="btn btn-secondary"
            onclick="copyToClipboard('tsTypes')"
          >
            📋 Copy TypeScript Types
          </button>
        </div>

        <!-- JSON Server Data -->
        <div class="section">
          <h2><span class="icon">⚡</span>JSON Server Data</h2>
          <p>Ready-to-use JSON file for json-server with normalized data:</p>
          <div class="code-block" id="jsonServerData"></div>
          <button
            class="btn btn-warning"
            onclick="copyToClipboard('jsonServerData')"
          >
            📋 Copy JSON Server Data
          </button>
          <button class="btn btn-secondary" onclick="downloadJson()">
            📥 Download db.json
          </button>
        </div>

        <!-- API Endpoints -->
        <div class="section">
          <h2><span class="icon">🚀</span>JSON Server API Endpoints</h2>
          <p>Available REST endpoints for your json-server:</p>
          <div class="grid">
            <div class="card">
              <h3>Chatbots</h3>
              <code>GET /chatagents</code><br />
              <code>GET /chatagents/:id</code><br />
              <code>POST /chatagents</code><br />
              <code>PUT /chatagents/:id</code><br />
              <code>DELETE /chatagents/:id</code>
            </div>
            <div class="card">
              <h3>Appearance</h3>
              <code>GET /chatagent_appearance</code><br />
              <code>GET /chatagent_appearance?chatagent_id=:id</code><br />
              <code>POST /chatagent_appearance</code><br />
              <code>PUT /chatagent_appearance/:id</code>
            </div>
            <div class="card">
              <h3>Behavior</h3>
              <code>GET /chatagent_behavior</code><br />
              <code>GET /chatagent_behavior?chatagent_id=:id</code><br />
              <code>POST /chatagent_behavior</code><br />
              <code>PUT /chatagent_behavior/:id</code>
            </div>
            <div class="card">
              <h3>Integrations</h3>
              <code>GET /chatagent_integrations</code><br />
              <code>GET /chatagent_integrations?chatagent_id=:id</code><br />
              <code>POST /chatagent_integrations</code><br />
              <code>PUT /chatagent_integrations/:id</code>
            </div>
            <div class="card">
              <h3>Stats</h3>
              <code>GET /chatagent_stats</code><br />
              <code>GET /chatagent_stats?chatagent_id=:id</code><br />
              <code>POST /chatagent_stats</code><br />
              <code>PUT /chatagent_stats/:id</code>
            </div>
            <div class="card">
              <h3>Training Data</h3>
              <code>GET /training_data</code><br />
              <code>GET /training_data?chatagent_id=:id</code><br />
              <code>POST /training_data</code><br />
              <code>PUT /training_data/:id</code>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script>
      // Original data transformation
      const originalData = [
        {
          id: "1",
          name: "Customer Support Bot",
          description: "A chatagent for handling customer support inquiries",
          aiModel: "gemini-pro",
          status: "active",
          createdAt: "2023-11-15T10:30:00Z",
          updatedAt: "2023-12-05T14:22:00Z",
          settings: {
            appearance: {
              primaryColor: "#0ea5e9",
              fontFamily: "Inter",
              chatIcon: "chat-bubble",
              welcomeMessage: "Hi there! How can I help you today?",
            },
            behavior: {
              initialMessages: [
                "Hi there! How can I help you today?",
                "You can ask me about our products, services, or support issues.",
              ],
              fallbackMessage:
                "I'm sorry, I don't understand that question. Could you rephrase it?",
              enableHumanHandoff: true,
              offlineMessage:
                "Our support team is currently offline. Please leave a message and we'll get back to you.",
            },
            integrations: {
              platforms: ["website", "whatsapp"],
              apiKeys: {
                whatsapp: "wa_api_key_123",
              },
            },
          },
          stats: {
            totalMessages: 1245,
            uniqueUsers: 342,
            averageRating: 4.7,
            responseRate: 0.92,
            conversionsCount: 56,
          },
          trainingData: [
            { type: "content", content: "mytraining data" },
            { type: "document", document: "my training data document" },
            { type: "website", content: "mytraining data from my site" },
          ],
        },
        {
          id: "2",
          name: "Sales Assistant",
          description:
            "A chatagent for helping with product recommendations and sales",
          aiModel: "gpt-4",
          status: "active",
          createdAt: "2023-10-20T09:15:00Z",
          updatedAt: "2023-12-01T11:45:00Z",
          settings: {
            appearance: {
              primaryColor: "#8b5cf6",
              fontFamily: "Lexend",
              chatIcon: "shopping-cart",
              welcomeMessage: "Hello! Looking for product recommendations?",
            },
            behavior: {
              initialMessages: [
                "Hello! Looking for product recommendations?",
                "I can help you find the perfect product based on your needs.",
              ],
              fallbackMessage:
                "I'm not sure I understand. Could you tell me more about what you're looking for?",
              enableHumanHandoff: true,
              offlineMessage:
                "Our sales team is currently unavailable. Please leave your contact information and we'll reach out to you.",
            },
            integrations: {
              platforms: ["website", "whatsapp", "shopify"],
              apiKeys: {
                whatsapp: "wa_api_key_456",
                shopify: "shopify_api_key_789",
              },
            },
          },
          stats: {
            totalMessages: 876,
            uniqueUsers: 215,
            averageRating: 4.5,
            responseRate: 0.88,
            conversionsCount: 42,
          },
          trainingData: [
            { type: "content", content: "mytraining data" },
            { type: "document", document: "my training data document" },
            { type: "website", content: "mytraining data from my site" },
          ],
        },
        {
          id: "3",
          name: "Product Recommender",
          description:
            "A chatagent for recommending products based on user preferences",
          aiModel: "claude-2",
          status: "draft",
          createdAt: "2023-12-01T15:20:00Z",
          updatedAt: "2023-12-01T15:20:00Z",
          settings: {
            appearance: {
              primaryColor: "#10b981",
              fontFamily: "Inter",
              chatIcon: "gift",
              welcomeMessage:
                "Hi! I can help you find the perfect product. What are you looking for today?",
            },
            behavior: {
              initialMessages: [
                "Hi! I can help you find the perfect product. What are you looking for today?",
              ],
              fallbackMessage:
                "I'm not sure I understand. Could you provide more details about what you're looking for?",
              enableHumanHandoff: false,
              offlineMessage:
                "Our product specialists are currently unavailable. Please leave a message and we'll get back to you.",
            },
            integrations: {
              platforms: ["website"],
              apiKeys: {},
            },
          },
          stats: {
            totalMessages: 0,
            uniqueUsers: 0,
            averageRating: 0,
            responseRate: 0,
            conversionsCount: 0,
          },
          trainingData: [
            { type: "content", content: "mytraining data" },
            { type: "document", document: "my training data document" },
            { type: "website", content: "mytraining data from my site" },
          ],
        },
      ];

      // Normalize data into separate tables
      function normalizeData() {
        const chatagents = [];
        const chatagentAppearance = [];
        const chatagentBehavior = [];
        const chatagentIntegrations = [];
        const chatagentStats = [];
        const trainingData = [];

        let appearanceId = 1;
        let behaviorId = 1;
        let integrationId = 1;
        let statsId = 1;
        let trainingId = 1;

        originalData.forEach((bot) => {
          // Main chatagent table
          chatagents.push({
            id: parseInt(bot.id),
            name: bot.name,
            description: bot.description,
            ai_model: bot.aiModel,
            status: bot.status,
            created_at: bot.createdAt,
            updated_at: bot.updatedAt,
          });

          // Appearance table
          chatagentAppearance.push({
            id: appearanceId++,
            chatagent_id: parseInt(bot.id),
            primary_color: bot.settings.appearance.primaryColor,
            font_family: bot.settings.appearance.fontFamily,
            chat_icon: bot.settings.appearance.chatIcon,
            welcome_message: bot.settings.appearance.welcomeMessage,
            created_at: bot.createdAt,
            updated_at: bot.updatedAt,
          });

          // Behavior table
          chatagentBehavior.push({
            id: behaviorId++,
            chatagent_id: parseInt(bot.id),
            initial_messages: JSON.stringify(
              bot.settings.behavior.initialMessages
            ),
            fallback_message: bot.settings.behavior.fallbackMessage,
            enable_human_handoff: bot.settings.behavior.enableHumanHandoff,
            offline_message: bot.settings.behavior.offlineMessage,
            created_at: bot.createdAt,
            updated_at: bot.updatedAt,
          });

          // Integrations table
          bot.settings.integrations.platforms.forEach((platform) => {
            chatagentIntegrations.push({
              id: integrationId++,
              chatagent_id: parseInt(bot.id),
              platform: platform,
              api_key: bot.settings.integrations.apiKeys[platform] || null,
              is_active: !!bot.settings.integrations.apiKeys[platform],
              created_at: bot.createdAt,
              updated_at: bot.updatedAt,
            });
          });

          // Stats table
          chatagentStats.push({
            id: statsId++,
            chatagent_id: parseInt(bot.id),
            total_messages: bot.stats.totalMessages,
            unique_users: bot.stats.uniqueUsers,
            average_rating: bot.stats.averageRating,
            response_rate: bot.stats.responseRate,
            conversions_count: bot.stats.conversionsCount,
            last_calculated_at: new Date().toISOString(),
          });

          // Enhanced training data
          const enhancedTrainingData = generateEnhancedTrainingData(bot);
          enhancedTrainingData.forEach((training) => {
            trainingData.push({
              id: trainingId++,
              chatagent_id: parseInt(bot.id),
              content_type: training.type,
              category: training.category,
              title: training.title,
              content: training.content,
              intent: training.intent,
              keywords: training.keywords,
              confidence_score: training.confidence,
              source_url: training.sourceUrl,
              is_active: true,
              created_at: bot.createdAt,
              updated_at: bot.updatedAt,
            });
          });
        });

        return {
          chatagents,
          chatagent_appearance: chatagentAppearance,
          chatagent_behavior: chatagentBehavior,
          chatagent_integrations: chatagentIntegrations,
          chatagent_stats: chatagentStats,
          training_data: trainingData,
        };
      }

      // Generate enhanced training data
      function generateEnhancedTrainingData(bot) {
        const trainingCategories = [
          {
            type: "faq",
            category: "Customer Support",
            title: "Product Return Policy",
            content:
              "Our return policy allows customers to return products within 30 days of purchase. Items must be in original condition with tags attached. Refunds are processed within 5-7 business days.",
            intent: "return_policy_inquiry",
            keywords: "return,refund,policy,30 days,original condition",
            confidence: 0.95,
            sourceUrl: null,
          },
          {
            type: "faq",
            category: "Customer Support",
            title: "Shipping Information",
            content:
              "We offer free shipping on orders over $50. Standard shipping takes 3-5 business days, express shipping takes 1-2 business days. International shipping is available to select countries.",
            intent: "shipping_inquiry",
            keywords: "shipping,delivery,free shipping,express,international",
            confidence: 0.92,
            sourceUrl: null,
          },
          {
            type: "knowledge_base",
            category: "Product Information",
            title: "Product Specifications",
            content:
              "Our premium headphones feature noise-cancelling technology, 20-hour battery life, wireless Bluetooth 5.0 connectivity, and come with a carrying case.",
            intent: "product_specifications",
            keywords: "headphones,noise-cancelling,battery,bluetooth,wireless",
            confidence: 0.88,
            sourceUrl: null,
          },
          {
            type: "procedure",
            category: "Support Process",
            title: "Troubleshooting Steps",
            content:
              "If experiencing connectivity issues: 1) Check device compatibility 2) Reset network settings 3) Update firmware 4) Contact support if issues persist",
            intent: "troubleshooting_guide",
            keywords: "troubleshooting,connectivity,reset,firmware,support",
            confidence: 0.9,
            sourceUrl: null,
          },
          {
            type: "external_link",
            category: "Documentation",
            title: "User Manual",
            content:
              "Complete user manual with setup instructions, troubleshooting guides, and warranty information.",
            intent: "documentation_access",
            keywords: "manual,setup,instructions,warranty,guide",
            confidence: 0.85,
            sourceUrl: "https://docs.example.com/user-manual",
          },
          {
            type: "website_content",
            category: "Company Information",
            title: "About Our Company",
            content:
              "We are a leading technology company specializing in innovative AI solutions and customer service automation. Founded in 2020, we serve over 10,000 businesses worldwide.",
            intent: "company_information",
            keywords: "company,about,technology,AI,automation,founded",
            confidence: 0.87,
            sourceUrl: "https://example.com/about",
          },
        ];

        return trainingCategories;
      }

      // Populate tables
      function populateTables() {
        const normalizedData = normalizeData();

        // Populate chatagents table
        const chatagentsBody = document.getElementById("chatagentsTableBody");
        chatagentsBody.innerHTML = "";
        normalizedData.chatagents.forEach((bot) => {
          const row = chatagentsBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${bot.id}</span></td>
                    <td><strong>${bot.name}</strong></td>
                    <td>${bot.description}</td>
                    <td><span class="data-type">${bot.ai_model}</span></td>
                    <td><span class="highlight">${bot.status}</span></td>
                    <td>${new Date(bot.created_at).toLocaleDateString()}</td>
                    <td>${new Date(bot.updated_at).toLocaleDateString()}</td>
                `;
        });

        // Populate appearance table
        const appearanceBody = document.getElementById("appearanceTableBody");
        appearanceBody.innerHTML = "";
        normalizedData.chatagent_appearance.forEach((appearance) => {
          const row = appearanceBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${appearance.id}</span></td>
                    <td>${appearance.chatagent_id}</td>
                    <td><div style="width:20px;height:20px;background:${appearance.primary_color};border-radius:4px;display:inline-block;margin-right:8px;"></div>${appearance.primary_color}</td>
                    <td>${appearance.font_family}</td>
                    <td>${appearance.chat_icon}</td>
                    <td>${appearance.welcome_message}</td>
                    <td>${new Date(appearance.created_at).toLocaleDateString()}</td>
                    <td>${new Date(appearance.updated_at).toLocaleDateString()}</td>
                `;
        });

        // Populate behavior table
        const behaviorBody = document.getElementById("behaviorTableBody");
        behaviorBody.innerHTML = "";
        normalizedData.chatagent_behavior.forEach((behavior) => {
          const row = behaviorBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${behavior.id}</span></td>
                    <td>${behavior.chatagent_id}</td>
                    <td>${behavior.initial_messages.length > 50 ? behavior.initial_messages.substring(0, 50) + "..." : behavior.initial_messages}</td>
                    <td>${behavior.fallback_message.length > 30 ? behavior.fallback_message.substring(0, 30) + "..." : behavior.fallback_message}</td>
                    <td>${behavior.enable_human_handoff ? "✅" : "❌"}</td>
                    <td>${behavior.offline_message.length > 30 ? behavior.offline_message.substring(0, 30) + "..." : behavior.offline_message}</td>
                    <td>${new Date(behavior.created_at).toLocaleDateString()}</td>
                    <td>${new Date(behavior.updated_at).toLocaleDateString()}</td>
                `;
        });

        // Populate integrations table
        const integrationsBody = document.getElementById(
          "integrationsTableBody"
        );
        integrationsBody.innerHTML = "";
        normalizedData.chatagent_integrations.forEach((integration) => {
          const row = integrationsBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${integration.id}</span></td>
                    <td>${integration.chatagent_id}</td>
                    <td><span class="data-type">${integration.platform}</span></td>
                    <td>${integration.api_key ? "🔐 " + integration.api_key.substring(0, 10) + "..." : "❌ Not configured"}</td>
                    <td>${integration.is_active ? "✅" : "❌"}</td>
                    <td>${new Date(integration.created_at).toLocaleDateString()}</td>
                    <td>${new Date(integration.updated_at).toLocaleDateString()}</td>
                `;
        });

        // Populate stats table
        const statsBody = document.getElementById("statsTableBody");
        statsBody.innerHTML = "";
        normalizedData.chatagent_stats.forEach((stat) => {
          const row = statsBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${stat.id}</span></td>
                    <td>${stat.chatagent_id}</td>
                    <td><span class="data-type">${stat.total_messages.toLocaleString()}</span></td>
                    <td><span class="data-type">${stat.unique_users.toLocaleString()}</span></td>
                    <td><span class="data-type">${stat.average_rating}/5</span></td>
                    <td><span class="data-type">${(stat.response_rate * 100).toFixed(1)}%</span></td>
                    <td><span class="data-type">${stat.conversions_count}</span></td>
                    <td>${new Date(stat.last_calculated_at).toLocaleDateString()}</td>
                `;
        });

        // Populate training table
        const trainingBody = document.getElementById("trainingTableBody");
        trainingBody.innerHTML = "";
        normalizedData.training_data.forEach((training) => {
          const row = trainingBody.insertRow();
          row.innerHTML = `
                    <td><span class="highlight">${training.id}</span></td>
                    <td>${training.chatagent_id}</td>
                    <td><span class="data-type">${training.content_type}</span></td>
                    <td>${training.category}</td>
                    <td><strong>${training.title}</strong></td>
                    <td>${training.content.length > 50 ? training.content.substring(0, 50) + "..." : training.content}</td>
                    <td>${training.intent}</td>
                    <td>${training.keywords}</td>
                    <td><span class="data-type">${training.confidence_score}</span></td>
                    <td>${training.source_url ? `<a href="${training.source_url}" target="_blank">🔗</a>` : "❌"}</td>
                    <td>${training.is_active ? "✅" : "❌"}</td>
                    <td>${new Date(training.created_at).toLocaleDateString()}</td>
                    <td>${new Date(training.updated_at).toLocaleDateString()}</td>
                `;
        });
      }

      // Generate SQL Schema
      function generateSQLSchema() {
        const sqlSchema = `-- Chatbots Database Schema
-- Generated for normalized chatagent data structure

-- Main chatagents table
CREATE TABLE chatagents (
    id INTEGER PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    ai_model VARCHAR(100) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'draft',
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    CONSTRAINT chk_status CHECK (status IN ('active', 'draft', 'inactive'))
);

-- Chatbot appearance settings
CREATE TABLE chatagent_appearance (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chatagent_id INTEGER NOT NULL,
    primary_color VARCHAR(7) NOT NULL, -- Hex color code
    font_family VARCHAR(100) NOT NULL,
    chat_icon VARCHAR(100) NOT NULL,
    welcome_message TEXT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (chatagent_id) REFERENCES chatagents(id) ON DELETE CASCADE,
    UNIQUE(chatagent_id) -- One-to-one relationship
);

-- Chatbot behavior settings
CREATE TABLE chatagent_behavior (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chatagent_id INTEGER NOT NULL,
    initial_messages JSON NOT NULL, -- Array of initial messages
    fallback_message TEXT NOT NULL,
    enable_human_handoff BOOLEAN NOT NULL DEFAULT false,
    offline_message TEXT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (chatagent_id) REFERENCES chatagents(id) ON DELETE CASCADE,
    UNIQUE(chatagent_id) -- One-to-one relationship
);

-- Chatbot platform integrations
CREATE TABLE chatagent_integrations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chatagent_id INTEGER NOT NULL,
    platform VARCHAR(100) NOT NULL,
    api_key VARCHAR(500), -- Encrypted API key
    is_active BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (chatagent_id) REFERENCES chatagents(id) ON DELETE CASCADE,
    UNIQUE(chatagent_id, platform) -- One chatagent can have one config per platform
);

-- Chatbot performance statistics
CREATE TABLE chatagent_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chatagent_id INTEGER NOT NULL,
    total_messages INTEGER NOT NULL DEFAULT 0,
    unique_users INTEGER NOT NULL DEFAULT 0,
    average_rating DECIMAL(3,2) DEFAULT 0.00, -- 0.00 to 5.00
    response_rate DECIMAL(5,4) DEFAULT 0.0000, -- 0.0000 to 1.0000
    conversions_count INTEGER NOT NULL DEFAULT 0,
    last_calculated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (chatagent_id) REFERENCES chatagents(id) ON DELETE CASCADE,
    UNIQUE(chatagent_id), -- One-to-one relationship
    CONSTRAINT chk_rating CHECK (average_rating >= 0 AND average_rating <= 5),
    CONSTRAINT chk_response_rate CHECK (response_rate >= 0 AND response_rate <= 1)
);

-- Enhanced training data for vectorization
CREATE TABLE training_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chatagent_id INTEGER NOT NULL,
    content_type VARCHAR(50) NOT NULL, -- faq, knowledge_base, procedure, external_link, website_content
    category VARCHAR(100) NOT NULL,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    intent VARCHAR(100) NOT NULL,
    keywords TEXT, -- Comma-separated keywords for search
    confidence_score DECIMAL(4,3) DEFAULT 0.000, -- 0.000 to 1.000
    source_url VARCHAR(500), -- URL for external content
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (chatagent_id) REFERENCES chatagents(id) ON DELETE CASCADE,
    CONSTRAINT chk_content_type CHECK (content_type IN ('faq', 'knowledge_base', 'procedure', 'external_link', 'website_content')),
    CONSTRAINT chk_confidence CHECK (confidence_score >= 0 AND confidence_score <= 1)
);

-- Indexes for better performance
CREATE INDEX idx_chatagents_status ON chatagents(status);
CREATE INDEX idx_chatagents_ai_model ON chatagents(ai_model);
CREATE INDEX idx_chatagent_appearance_chatagent_id ON chatagent_appearance(chatagent_id);
CREATE INDEX idx_chatagent_behavior_chatagent_id ON chatagent_behavior(chatagent_id);
CREATE INDEX idx_chatagent_integrations_chatagent_id ON chatagent_integrations(chatagent_id);
CREATE INDEX idx_chatagent_integrations_platform ON chatagent_integrations(platform);
CREATE INDEX idx_chatagent_stats_chatagent_id ON chatagent_stats(chatagent_id);
CREATE INDEX idx_training_data_chatagent_id ON training_data(chatagent_id);
CREATE INDEX idx_training_data_content_type ON training_data(content_type);
CREATE INDEX idx_training_data_category ON training_data(category);
CREATE INDEX idx_training_data_intent ON training_data(intent);
CREATE INDEX idx_training_data_is_active ON training_data(is_active);

-- Full-text search index for training data content
CREATE VIRTUAL TABLE training_data_fts USING fts5(
    title, content, keywords,
    content=training_data,
    content_rowid=id
);

-- Triggers to maintain FTS index
CREATE TRIGGER training_data_fts_insert AFTER INSERT ON training_data BEGIN
    INSERT INTO training_data_fts(rowid, title, content, keywords)
    VALUES (new.id, new.title, new.content, new.keywords);
END;

CREATE TRIGGER training_data_fts_delete AFTER DELETE ON training_data BEGIN
    DELETE FROM training_data_fts WHERE rowid = old.id;
END;

CREATE TRIGGER training_data_fts_update AFTER UPDATE ON training_data BEGIN
    DELETE FROM training_data_fts WHERE rowid = old.id;
    INSERT INTO training_data_fts(rowid, title, content, keywords)
    VALUES (new.id, new.title, new.content, new.keywords);
END;`;

        document.getElementById("sqlSchema").textContent = sqlSchema;
      }

      // Generate TypeScript Types
      function generateTypeScriptTypes() {
        const tsTypes = `// TypeScript type definitions for normalized chatagent data
// Generated from normalized database schema

export interface Chatbot {
  id: number;
  name: string;
  description: string;
  ai_model: string;
  status: 'active' | 'draft' | 'inactive';
  created_at: string; // ISO 8601 datetime string
  updated_at: string; // ISO 8601 datetime string
}

export interface ChatbotAppearance {
  id: number;
  chatagent_id: number;
  primary_color: string; // Hex color code
  font_family: string;
  chat_icon: string;
  welcome_message: string;
  created_at: string;
  updated_at: string;
}

export interface ChatbotBehavior {
  id: number;
  chatagent_id: number;
  initial_messages: string; // JSON string array
  fallback_message: string;
  enable_human_handoff: boolean;
  offline_message: string;
  created_at: string;
  updated_at: string;
}

export interface ChatbotIntegration {
  id: number;
  chatagent_id: number;
  platform: string;
  api_key: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}

export interface ChatbotStats {
  id: number;
  chatagent_id: number;
  total_messages: number;
  unique_users: number;
  average_rating: number;
  response_rate: number;
  conversions_count: number;
  last_calculated_at: string;
}

export interface TrainingData {
  id: number;
  chatagent_id: number;
  content_type: 'faq' | 'knowledge_base' | 'procedure' | 'external_link' | 'website_content';
  category: string;
  title: string;
  content: string;
  intent: string;
  keywords: string; // Comma-separated keywords
  confidence_score: number;
  source_url: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}

// Parsed types for better type safety
export interface ChatbotBehaviorParsed extends Omit<ChatbotBehavior, 'initial_messages'> {
  initial_messages: string[]; // Parsed JSON array
}

export interface TrainingDataWithKeywords extends Omit<TrainingData, 'keywords'> {
  keywords: string[]; // Parsed keywords array
}

// API Response types
export interface ApiResponse<T> {
  data: T;
  message?: string;
  status: 'success' | 'error';
}

export interface PaginatedResponse<T> {
  data: T[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
  };
}

// Request/Update types
export interface CreateChatbotRequest {
  name: string;
  description: string;
  ai_model: string;
  status?: 'active' | 'draft' | 'inactive';
}

export interface UpdateChatbotRequest extends Partial<CreateChatbotRequest> {
  id: number;
}

export interface CreateTrainingDataRequest {
  chatagent_id: number;
  content_type: TrainingData['content_type'];
  category: string;
  title: string;
  content: string;
  intent: string;
  keywords: string;
  confidence_score?: number;
  source_url?: string;
}

// Utility types
export type ChatbotWithRelations = Chatbot & {
  appearance?: ChatbotAppearance;
  behavior?: ChatbotBehavior;
  integrations?: ChatbotIntegration[];
  stats?: ChatbotStats;
  training_data?: TrainingData[];
};

// Enums for better type safety
export enum ChatbotStatus {
  ACTIVE = 'active',
  DRAFT = 'draft',
  INACTIVE = 'inactive'
}

export enum ContentType {
  FAQ = 'faq',
  KNOWLEDGE_BASE = 'knowledge_base',
  PROCEDURE = 'procedure',
  EXTERNAL_LINK = 'external_link',
  WEBSITE_CONTENT = 'website_content'
}

export enum Platform {
  WEBSITE = 'website',
  WHATSAPP = 'whatsapp',
  SHOPIFY = 'shopify',
  TELEGRAM = 'telegram',
  SLACK = 'slack'
}`;

        document.getElementById("tsTypes").textContent = tsTypes;
      }

      // Generate JSON Server Data
      function generateJSONServerData() {
        const normalizedData = normalizeData();
        const jsonServerData = JSON.stringify(normalizedData, null, 2);
        document.getElementById("jsonServerData").textContent = jsonServerData;
        return normalizedData;
      }

      // Tab functionality
      function showTab(tabName) {
        // Hide all tab contents
        const tabContents = document.querySelectorAll(".tab-content");
        tabContents.forEach((content) => content.classList.remove("active"));

        // Remove active class from all tabs
        const tabs = document.querySelectorAll(".tab");
        tabs.forEach((tab) => tab.classList.remove("active"));

        // Show selected tab content
        document.getElementById(tabName + "-tab").classList.add("active");

        // Add active class to clicked tab
        event.target.classList.add("active");
      }

      // Copy to clipboard function
      function copyToClipboard(elementId) {
        const element = document.getElementById(elementId);
        const text = element.textContent;

        navigator.clipboard
          .writeText(text)
          .then(() => {
            // Visual feedback
            const originalText = event.target.textContent;
            event.target.textContent = "✅ Copied!";
            event.target.style.background =
              "linear-gradient(135deg, #10b981, #059669)";

            setTimeout(() => {
              event.target.textContent = originalText;
              event.target.style.background = "";
            }, 2000);
          })
          .catch((err) => {
            console.error("Failed to copy: ", err);
            alert("Failed to copy to clipboard");
          });
      }

      // Download JSON file
      function downloadJson() {
        const normalizedData = normalizeData();
        const jsonString = JSON.stringify(normalizedData, null, 2);
        const blob = new Blob([jsonString], { type: "application/json" });
        const url = URL.createObjectURL(blob);

        const a = document.createElement("a");
        a.href = url;
        a.download = "db.json";
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
      }

      // Initialize the page
      document.addEventListener("DOMContentLoaded", function () {
        populateTables();
        generateSQLSchema();
        generateTypeScriptTypes();
        generateJSONServerData();
      });
    </script>
  </body>
</html>
